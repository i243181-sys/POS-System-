console.error('The legacy local HTTP launcher is disabled for security.')
console.error('Use the maintained Electron IPC app instead: npm run dev, npm run start, or npm run package.')
process.exit(1)
